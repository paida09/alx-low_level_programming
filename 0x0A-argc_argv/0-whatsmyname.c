#include <stdio.h>
#include "main.h"

/**
  * main - Prints name of program
  * @argc: argument count
  * @argv: array of arguments
  *
  * Return: Always zero
  */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
